const MEDICINES = ['Paracetamol 500mg','Amoxicillin 500mg','Metformin 500mg','Atorvastatin 10mg','Cetirizine 10mg','Azithromycin 500mg','Amlodipine 5mg','Omeprazole 20mg','Ciprofloxacin 500mg','Ibuprofen 400mg'];
const KENDRAS = [['K1','Sector 18, Noida'],['K2','Greater Noida'],['K3','Dadri'],['K4','Ghaziabad'],['K5','Meerut'],['K6','Bulandshahr'],['K7','Hapur']];
const anomaly = {
 'K1-Amlodipine 5mg':{dispatched:110,sold:88,written:22,remaining:12,flags:['Stock Balance','Write-off ratio'],risk:'High',note:'Reported stock does not reconcile'},
 'K1-Ibuprofen 400mg':{dispatched:130,sold:95,written:28,remaining:7,flags:['Write-off ratio'],risk:'Low',note:'Write-off rate 21.5% exceeds threshold'},
 'K2-Paracetamol 500mg':{dispatched:120,sold:84,written:6,remaining:15,flags:['Stock Balance'],risk:'High',note:'15 units reported; 30 expected'},
 'K3-Metformin 500mg':{dispatched:150,sold:60,written:70,remaining:25,flags:['Stock Balance','Write-off ratio'],risk:'High',note:'46.7% written off; stock mismatch'},
 'K4-Omeprazole 20mg':{dispatched:95,sold:40,written:5,remaining:50,flags:['Dormant stock'],risk:'Low',note:'No sale recorded for 23 days'},
 'K5-Azithromycin 500mg':{dispatched:80,sold:55,written:5,remaining:20,expiry:'2026-06-01',lastSale:'2026-08-25',flags:['Expired-stock sale','Dormant stock'],risk:'Medium',note:'Stock sold after expiry; velocity drop'},
 'K6-Amoxicillin 500mg':{dispatched:100,sold:70,written:5,remaining:25,expiry:'2026-07-01',flags:['Expired-stock sale'],risk:'Low',note:'Sale recorded after batch expiry'},
 'K7-Atorvastatin 10mg':{dispatched:98,sold:40,written:49,remaining:9,flags:['Write-off ratio'],risk:'Low',note:'50% write-off ratio'},
 'K7-Cetirizine 10mg':{dispatched:140,sold:50,written:10,remaining:100,expiry:'2026-05-01',lastSale:'2026-08-20',flags:['Stock Balance','Expired-stock sale','Dormant stock'],risk:'High',note:'Critical: 3 rules triggered'}
};
const inventory = KENDRAS.flatMap(([id,kendra],ki)=>MEDICINES.map((medicine,mi)=>{const base={id,kendra,medicine,dispatched:90+ki*8+mi*4,sold:60+ki*7+mi*3,written:3+(mi%6),expiry:`2027-${String((mi+3)%12+1).padStart(2,'0')}-01`,lastSale:`2026-08-${String(27+(mi%5)).padStart(2,'0')}`,flags:[],risk:'None',note:'Reconciled'};base.remaining=base.dispatched-base.sold-base.written;return {...base,...(anomaly[`${id}-${medicine}`]||{})}}));
let state={view:'admin',search:'',lastBill:null,batches:[{batch:'BT-AX-260901',medicine:'Paracetamol 500mg',quantity:600,expiry:'2028-08-31',kendra:'K1',status:'Received'},{batch:'BT-MT-260831',medicine:'Metformin 500mg',quantity:400,expiry:'2028-07-31',kendra:'K3',status:'In transit'}]};
const $=s=>document.querySelector(s); const root=$('#view-root');
function flags(){return inventory.filter(r=>r.flags.length)} function riskClass(r){return r.risk.toLowerCase()}
function toast(message){const el=$('#toast');el.textContent=message;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),3200)}
function setView(view){state.view=view;document.querySelectorAll('.nav-item').forEach(n=>n.classList.toggle('active',n.dataset.view===view));render()}
function esc(x){return String(x).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}
function badge(r){return `<span class="badge ${riskClass(r)}">${r.risk.toUpperCase()}</span>`}
function pageHead(eyebrow,title,subtitle,action=''){return `<div class="page-head"><div><div class="eyebrow">${eyebrow}</div><h1>${title}</h1><p>${subtitle}</p></div>${action}</div>`}
function admin(){const list=flags();return `<div class="page">${pageHead('OVERVIEW / 02 SEP 2026','Good afternoon, Coordinator.','Here is the integrity pulse across your monitored kendras.','<button class="primary-btn" data-action="batch">＋ Create batch</button>')}<div class="stats"><div class="stat-card" style="--accent:#41ae92"><span class="label">Kendras monitored</span><strong>7</strong><small><span class="delta">↑ 2</span> in demo coverage</small></div><div class="stat-card" style="--accent:#47b4d1"><span class="label">Medicine records</span><strong>70</strong><small>Across 10 priority SKUs</small></div><div class="stat-card" style="--accent:#f9be3c"><span class="label">Open risk flags</span><strong>${list.length}</strong><small><span class="attention">3 high priority</span> need review</small></div><div class="stat-card" style="--accent:#ee7770"><span class="label">Integrity score</span><strong>86.4%</strong><small><span class="delta">↑ 1.8%</span> vs. prior period</small></div></div><div class="grid-main"><div class="card"><div class="card-title"><div><h2>Priority review queue</h2><p>Ranked by explainable rule severity</p></div><button class="link-btn" data-view-link="flags">View all →</button></div><div class="risk-list">${list.slice(0,5).map(r=>`<div class="risk-row"><i class="risk-bar" style="--risk:${r.risk==='High'?'#e25150':r.risk==='Medium'?'#f9be3c':'#a3cc54'}"></i><div><strong>${r.kendra} · ${r.medicine}</strong><br><span>${r.flags.join(' · ')}</span></div>${badge(r)}</div>`).join('')}</div></div><div class="card"><div class="card-title"><div><h2>Risk distribution</h2><p>Flagged records only</p></div></div><div class="donut-wrap"><div class="donut"></div><div class="donut-label"><b>${list.length}</b><small>OPEN FLAGS</small></div></div><div class="legend"><div><span style="--c:#e25150">High priority</span><b>4</b></div><div><span style="--c:#f9be3c">Medium review</span><b>1</b></div><div><span style="--c:#beeb53">Low monitor</span><b>4</b></div></div></div></div><div class="ledger-strip"><span class="lock">▣</span><div><b>Ledger integrity: verified</b><p>Every demo movement is timestamped and write-protected.</p></div><small>LAST SEAL 14:06 IST</small></div></div>`}
function flagsView(){let rows=flags().filter(r=>`${r.kendra} ${r.medicine} ${r.risk}`.toLowerCase().includes(state.search.toLowerCase()));return `<div class="page">${pageHead('ADMINISTRATION / RISK INBOX','Explainable risk inbox','Only records with rule breaches are shown. Each flag retains its supporting numbers.','<button class="secondary-btn" data-action="export">⇩ Export review list</button>')}<div class="card table-card"><div class="table-toolbar"><div><strong>${rows.length} flagged records</strong><small style="margin-left:8px;color:#758886">Synthetic demo data · reference date 02 Sep 2026</small></div><input class="search" id="flag-search" value="${esc(state.search)}" placeholder="Search kendra or medicine" /></div><table><thead><tr><th>Kendra</th><th>Medicine / batch health</th><th>Reconciliation</th><th>Triggered rules</th><th>Risk</th><th></th></tr></thead><tbody>${rows.map(r=>`<tr class="flagged"><td><b>${r.kendra}</b><br><small class="mono">${r.id}</small></td><td><b>${r.medicine}</b><br><small>Expiry ${r.expiry} · ${r.note}</small></td><td><span class="qty">${r.dispatched} / ${r.sold} / ${r.written} / ${r.remaining}</span><br><small>Dispatch / sale / write-off / reported</small></td><td>${r.flags.map(x=>`<span class="badge ${riskClass(r)}">${x}</span>`).join(' ')}</td><td>${badge(r)}</td><td><button class="link-btn" data-action="review" data-item="${r.id}|${r.medicine}">Review</button></td></tr>`).join('')}</tbody></table></div></div>`}
function batches(){return `<div class="page">${pageHead('ADMINISTRATION / BATCH REGISTRY','Batch custody & traceability','Create dispatch batches, bind them to a kendra and retain their history.','<button class="primary-btn" data-action="batch">＋ Create batch</button>')}<div class="split"><div class="card table-card"><div class="table-toolbar"><strong>Active batch records</strong><span class="mono">${state.batches.length} batches</span></div><table><thead><tr><th>Batch ID</th><th>Medicine</th><th>Quantity</th><th>Expiry</th><th>Destination</th><th>Status</th></tr></thead><tbody>${state.batches.map(b=>`<tr><td class="mono">${b.batch}</td><td><b>${b.medicine}</b></td><td>${b.quantity} units</td><td>${b.expiry}</td><td>${b.kendra}</td><td><span class="badge low">${b.status.toUpperCase()}</span></td></tr>`).join('')}</tbody></table></div><div class="card"><div class="card-title"><div><h2>Chain of custody</h2><p>Prototype flow</p></div></div><div class="rule-box"><h3>Every batch carries:</h3><div><b>01 · Batch identity</b><br>Batch number, SKU, expiry, quantity and dispatch source.</div><div><b>02 · Recipient acknowledgement</b><br>Receiver confirms arrival against the QR-tagged batch.</div><div><b>03 · Immutable event trail</b><br>Dispatch, receipt, sale and write-off events are retained for audit.</div></div></div></div></div>`}
function vendor(){return `<div class="page">${pageHead('KENDRA PORTAL / VENDOR COUNTER','Create a sale & verified bill','Each bill updates the sale ledger and gives the customer a QR-verifiable receipt.')}<div class="split"><div class="card"><div class="card-title"><div><h2>New point-of-sale bill</h2><p>Demo mode — SMS is simulated safely</p></div></div><form id="sale-form" class="form-grid"><div class="form-field"><label>Kendra</label><select name="kendra">${KENDRAS.map(([id,n])=>`<option value="${id}">${n} (${id})</option>`).join('')}</select></div><div class="form-field"><label>Medicine</label><select name="medicine">${MEDICINES.map(x=>`<option>${x}</option>`).join('')}</select></div><div class="form-field"><label>Quantity</label><input name="quantity" type="number" min="1" value="1" required /></div><div class="form-field"><label>Unit price (₹)</label><input name="price" type="number" min="1" value="18" required /></div><div class="form-field"><label>Customer name</label><input name="name" placeholder="e.g. Aarav Sharma" required /></div><div class="form-field"><label>Mobile number</label><input name="phone" inputmode="numeric" pattern="[0-9]{10}" placeholder="10-digit mobile number" required /></div><div class="form-field full"><label><input type="checkbox" name="consent" required /> I have obtained the customer's consent to send this bill by SMS.</label></div><div class="form-field full"><button class="primary-btn" type="submit">Generate bill & send SMS</button></div></form></div><div class="card"><div class="card-title"><div><h2>Built-in safeguards</h2><p>Rule checks before the bill is recorded</p></div></div><div class="rule-box"><div><b>Expiry check</b><br>Blocks sale if a batch has crossed its expiry date.</div><div><b>Inventory reconciliation</b><br>Sale event updates expected stock and audit history.</div><div><b>Customer privacy</b><br>Mobile number is used only for the transactional bill in this prototype.</div><div><b>QR receipt</b><br>Customer can validate billed medicine and timestamp from the receiver portal.</div></div></div></div>${state.lastBill?billReceipt(state.lastBill):''}</div>`}
function billReceipt(b){return `<div class="modal-backdrop"><div class="receipt"><div class="receipt-top"><div><h3>AushadhiTrack</h3><small>Verified Jan Aushadhi receipt</small></div><span class="mono">${b.id}</span></div><div class="receipt-row"><span>Customer</span><b>${esc(b.name)}</b></div><div class="receipt-row"><span>Medicine</span><b>${b.medicine}</b></div><div class="receipt-row"><span>Quantity</span><b>${b.quantity}</b></div><div class="receipt-row"><span>Kendra</span><b>${b.kendra}</b></div><div class="receipt-total receipt-row"><span>Total paid</span><span>₹${b.total}</span></div><div class="qr"></div><p style="text-align:center;font-size:10px;color:#64767b">Scan at AushadhiTrack to verify this purchase.</p><div class="modal-actions"><button class="secondary-btn" data-action="close-bill">Close</button><button class="primary-btn" data-action="sms">✓ SMS sent</button></div></div></div>`}
function receiver(){return `<div class="page verify">${pageHead('CITIZEN PORTAL / RECEIVER','Verify a medicine purchase','Scan the QR code on a bill to confirm its transaction status.')}<div class="card"><div class="eyebrow">NO LOGIN REQUIRED</div><h2 style="margin:8px 0">Check your purchase</h2><p style="color:var(--muted);font-size:11px">The scanner verifies the medicine, kendra, bill timestamp and the receipt’s audit reference.</p><div class="scanner">⌗<span>Point camera at AushadhiTrack QR</span></div><button class="primary-btn" data-action="scan">Simulate QR scan</button><div class="verify-result hidden" id="verify-result"><b>✓ Bill verified</b><br><span id="verify-text">No receipt scanned yet.</span></div></div><div class="card" style="margin-top:18px;text-align:left"><div class="card-title"><div><h2>Privacy and future identity protection</h2><p>Proposed for the production phase</p></div></div><p style="font-size:11px;line-height:1.7;color:var(--muted)">Aadhaar must never be used as a public identifier or stored unnecessarily. A production rollout would use explicit consent, minimal tokenised identity references, access controls, encryption, and compliance review before any Aadhaar-linked service is introduced.</p></div></div>`}
function render(){const map={admin,flags:flagsView,batches,vendor,receiver};root.innerHTML=map[state.view]();$('#breadcrumbs').textContent=({admin:'ADMINISTRATION / COMMAND CENTRE',flags:'ADMINISTRATION / RISK INBOX',batches:'ADMINISTRATION / BATCH REGISTRY',vendor:'KENDRA PORTAL / VENDOR COUNTER',receiver:'CITIZEN PORTAL / RECEIVER'})[state.view];document.querySelectorAll('[data-role]').forEach(b=>b.classList.toggle('active',b.dataset.role===state.view));bind()}
function openBatch(){const modal=$('#modal');$('#modal-content').innerHTML=`<h2>Create dispatch batch</h2><p>Register a traceable stock movement for the demo ledger.</p><form id="batch-form" class="form-grid"><div class="form-field"><label>Medicine</label><select name="medicine">${MEDICINES.map(x=>`<option>${x}</option>`).join('')}</select></div><div class="form-field"><label>Quantity</label><input name="quantity" type="number" min="1" value="300" required></div><div class="form-field"><label>Expiry date</label><input name="expiry" type="date" value="2028-09-01" required></div><div class="form-field"><label>Destination Kendra</label><select name="kendra">${KENDRAS.map(([id,n])=>`<option value="${id}">${n}</option>`).join('')}</select></div><div class="modal-actions form-field full"><button type="button" class="secondary-btn" data-action="close-modal">Cancel</button><button class="primary-btn">Add sealed batch</button></div></form>`;modal.classList.remove('hidden');$('#batch-form').onsubmit=e=>{e.preventDefault();const d=new FormData(e.target);state.batches.unshift({batch:`BT-AT-${Date.now().toString().slice(-6)}`,medicine:d.get('medicine'),quantity:d.get('quantity'),expiry:d.get('expiry'),kendra:d.get('kendra'),status:'Dispatched'});modal.classList.add('hidden');toast('Batch created and sealed in the prototype ledger.');state.view='batches';render()}}
function bind(){document.querySelectorAll('[data-view-link]').forEach(x=>x.onclick=()=>setView(x.dataset.viewLink));document.querySelectorAll('[data-action]').forEach(el=>el.onclick=()=>{const a=el.dataset.action;if(a==='batch')openBatch(); if(a==='export')toast('Review list prepared — export connector comes in production.');if(a==='review'){const [id,med]=el.dataset.item.split('|');const r=inventory.find(x=>x.id===id&&x.medicine===med);toast(`${r.kendra}: ${r.note}`)}if(a==='close-modal')$('#modal').classList.add('hidden');if(a==='close-bill'){state.lastBill=null;render()}if(a==='sms')toast('SMS receipt recorded for this demo.');if(a==='scan'){const v=$('#verify-result');v.classList.remove('hidden');v.querySelector('#verify-text').textContent=state.lastBill?`${state.lastBill.medicine} · ${state.lastBill.quantity} unit(s) · ${state.lastBill.kendra} · ₹${state.lastBill.total}`:'Demo receipt verified: Paracetamol 500mg · Sector 18, Noida · Bill AT-DEMO-260902';}});const search=$('#flag-search');if(search)search.oninput=e=>{state.search=e.target.value;render();setTimeout(()=>$('#flag-search').focus(),0)};const form=$('#sale-form');if(form)form.onsubmit=e=>{e.preventDefault();const d=new FormData(form);let phone=d.get('phone').replace(/\D/g,'');if(phone.length!==10){toast('Enter a valid 10-digit mobile number.');return}const kendra=KENDRAS.find(x=>x[0]===d.get('kendra'))[1];state.lastBill={id:`AT-${Date.now().toString().slice(-6)}`,name:d.get('name'),phone,medicine:d.get('medicine'),quantity:d.get('quantity'),kendra,total:Number(d.get('quantity'))*Number(d.get('price'))};toast('Bill saved; consented SMS queued in demo mode.');render()}}
document.querySelectorAll('.nav-item').forEach(n=>n.onclick=()=>setView(n.dataset.view));document.querySelectorAll('[data-role]').forEach(b=>b.onclick=()=>setView(b.dataset.role));render();
